# Local development uses the repository-root `.env`.
# Copy `../../../.env.example` to `../../../.env` and run from the repo root:
#
#   npm run dev
#
# The root dev script can read `ZEXVRO_STELLAR_IDENTITY` from Stellar CLI at
# runtime, so the sponsor secret does not need to be written into this folder.
