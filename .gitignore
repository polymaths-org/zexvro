node_modules/
dist/
coverage/
test-results/
playwright-report/
.DS_Store
*.log
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Local machine secrets/config. Keep `.env.example` committed as the template.
.env*
!.env.example

# Local runtime / agent data (may contain chat history or secrets)
.data/
*.db
*.sqlite
*.sqlite3
services/depin/depin.config.json

# Lakebed local artifacts
.lakebed/
demos/**/.lakebed/

# Morph MCP local install
services/morph/mcp/node_modules/

# Packaging dumps / accidental archives (never commit secrets inside zips)
*.zip
!packages/**/*.zip

# Large local circuit artifacts (use backups/zk-proofs-LATEST when needed)
zer0-pool/circuit/build/
zer0-pool/circuit/circomlib/
zer0-pool/circuit/*.ptau
zer0-pool/target/

# Gate captcha photo bank (local/S3 only — never commit blobs)
services/agent-auth/captcha-assets

# Kaggle credentials (never commit)
kaggle.json
.kaggle/

# Playwright CLI local snapshots
.playwright-cli/
