# Local development uses the repository-root `.env`.
# Copy `../.env.example` to `../.env` and run commands from the repo root:
#
#   npm run dev
#
# Keep this file only as a compatibility pointer for older frontend-only flows.
